from __future__ import annotationsfrom app.exchanges.registry import get_exchangefrom app.local.settings.store import LocalSettingsStorefrom app.local.signal_client.client import LocalSignalClientfrom app.local.signal_client.store import LocalSignalStorefrom app.local.trading.auto_trader import LocalAutoTraderfrom app.local.trading.log_store import LocalTradeStoresettings_store = LocalSettingsStore()signal_store = LocalSignalStore()trade_store = LocalTradeStore()auto_trader = LocalAutoTrader(settings_store, trade_store)signal_client = LocalSignalClient(settings_store, signal_store, auto_trader)def selected_exchange(name: str | None = None):    settings = settings_store.load()    return get_exchange(settings, name)